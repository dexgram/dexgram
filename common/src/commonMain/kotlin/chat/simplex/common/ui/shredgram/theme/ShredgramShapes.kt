package chat.simplex.common.ui.shredgram.theme

import androidx.compose.foundation.shape.RoundedCornerShape
import androidx.compose.ui.unit.dp

/**
 * Design token corner radius values for the Shredgram UI system.
 */

// Pill shape (fully rounded)
val RadiusPill = RoundedCornerShape(360.dp)

// Extra small radius
val RadiusXS = RoundedCornerShape(4.dp)

// Small radius
val RadiusSmall = RoundedCornerShape(6.dp)

// Medium radius
val RadiusMedium = RoundedCornerShape(8.dp)

// Large radius
val RadiusLarge = RoundedCornerShape(16.dp)

// Extra large radius
val RadiusXL = RoundedCornerShape(24.dp)

// Circle shape (50% of the container)
val RadiusCircle = RoundedCornerShape(50)

/**
 * Standard shape definitions for Material components
 */
object ShredgramShapes {
    val small = RoundedCornerShape(4.dp)
    val medium = RoundedCornerShape(8.dp)
    val large = RoundedCornerShape(12.dp)
    val extraLarge = RoundedCornerShape(16.dp)
}

